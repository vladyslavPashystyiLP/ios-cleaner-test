<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IosCleanerTest.MainPage"
             Title="Cleaner Test">

    <ContentPage.Resources>
        <Style x:Key="SectionBorder" TargetType="Border">
            <Setter Property="Stroke" Value="LightGray" />
            <Setter Property="StrokeShape" Value="RoundRectangle 10" />
            <Setter Property="Padding" Value="12" />
        </Style>
        <Style x:Key="CategoryTitle" TargetType="Label">
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="FontSize" Value="16" />
        </Style>
        <Style x:Key="ResultLabel" TargetType="Label">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="TextColor" Value="Gray" />
            <Setter Property="LineBreakMode" Value="WordWrap" />
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">

            <Label Text="Level 1 — Metadata scan"
                   FontSize="20" FontAttributes="Bold" />

            <Label x:Name="AccessStatusLabel" Text="Photo library access: not requested"
                   Style="{StaticResource ResultLabel}" />

            <Border Style="{StaticResource SectionBorder}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Screenshots" Style="{StaticResource CategoryTitle}" />
                    <HorizontalStackLayout Spacing="8">
                        <Button Text="Scan" Clicked="OnScanScreenshots" />
                        <Button Text="Add test data" Clicked="OnSeedScreenshot" />
                    </HorizontalStackLayout>
                    <Label x:Name="ScreenshotsResult" Style="{StaticResource ResultLabel}" Text="—" />
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource SectionBorder}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Live Photos" Style="{StaticResource CategoryTitle}" />
                    <HorizontalStackLayout Spacing="8">
                        <Button Text="Scan" Clicked="OnScanLivePhotos" />
                        <Button Text="Add test data" Clicked="OnSeedLivePhoto" />
                    </HorizontalStackLayout>
                    <Label x:Name="LivePhotosResult" Style="{StaticResource ResultLabel}" Text="—" />
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource SectionBorder}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Large videos (10+ MB)" Style="{StaticResource CategoryTitle}" />
                    <HorizontalStackLayout Spacing="8">
                        <Button Text="Scan" Clicked="OnScanLargeVideos" />
                        <Button Text="Add test data" Clicked="OnSeedLargeVideo" />
                    </HorizontalStackLayout>
                    <Label x:Name="LargeVideosResult" Style="{StaticResource ResultLabel}" Text="—" />
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource SectionBorder}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Heaviest files (top 10)" Style="{StaticResource CategoryTitle}" />
                    <HorizontalStackLayout Spacing="8">
                        <Button Text="Scan" Clicked="OnScanHeaviest" />
                        <Button Text="Add test data" Clicked="OnSeedHeavyImages" />
                    </HorizontalStackLayout>
                    <Label x:Name="HeaviestResult" Style="{StaticResource ResultLabel}" Text="—" />
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
