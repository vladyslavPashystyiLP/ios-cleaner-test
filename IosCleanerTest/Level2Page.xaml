<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IosCleanerTest.Level2Page"
             Title="Рівень 2">

    <ContentPage.Resources>
        <Style x:Key="SectionBorder" TargetType="Border">
            <Setter Property="Stroke" Value="LightGray" />
            <Setter Property="StrokeShape" Value="RoundRectangle 10" />
            <Setter Property="Padding" Value="12" />
        </Style>
        <Style x:Key="CategoryTitle" TargetType="Label">
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="FontSize" Value="16" />
        </Style>
        <Style x:Key="ResultLabel" TargetType="Label">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="TextColor" Value="Gray" />
            <Setter Property="LineBreakMode" Value="WordWrap" />
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">

            <Label Text="Рівень 2 — точні дублікати (dHash)"
                   FontSize="20" FontAttributes="Bold" />

            <Label Text="Перцептивний хеш прев'ю 9x8; збіг з відстанню Геммінга ≤ 6 = копія. Перший файл групи вважається оригіналом і в результат не потрапляє."
                   Style="{StaticResource ResultLabel}" />

            <Border Style="{StaticResource SectionBorder}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Дублікати фото" Style="{StaticResource CategoryTitle}" />
                    <HorizontalStackLayout Spacing="8">
                        <Button Text="Сканувати" Clicked="OnScanDuplicates" />
                        <Button Text="Додати тестові" Clicked="OnSeedDuplicates" />
                    </HorizontalStackLayout>
                    <Label x:Name="DuplicatesResult" Style="{StaticResource ResultLabel}" Text="—" />
                    <VerticalStackLayout x:Name="DuplicatesList" Spacing="8" />
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
